int n1 = h1 - l1 + 1;
    // int n2 = h2 - l2 + 1;
    // int *L = new int[n1];
    // int *R = new int[n2];

    // int i, j;
    // for (i = 0; i < n1; i++) {
    //     L[i] = arr[l1 + i];
    // } 
    // for (j = 0; j < n2; j++) {
    //     R[j] = arr[l2 + j];
    // }

    // int currentPos = l1;
    // i = 0;
    // j = 0;
    // while (i < n1 && j < n2) {
    //     if (L[i] < R[j]) {
    //         arr[currentPos++] = L[i++];
    //     }
    //     else {
    //         arr[currentPos++] = R[j++];
    //     }
    // }

    // while (i < n1) {
    //     arr[currentPos++] = L[i++];
    // }
    // while (j < n2) {
    //     arr[currentPos++] = R[j++];
    // }

    // delete(L);
    // delete(R);